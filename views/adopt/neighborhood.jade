extends ../layout

block content
  .page-header
    h1 
        | #{user.profile.city}&nbsp;
    span.h3 Neighborhood Dashboard
  .row
    .col-md-6
      h2 Your Pledges
      table.table.table-hover
        thead
          tr
            th Problem
            th Location
            th Time Submitted
            th View
        tbody
          tr
            td
              a(href='/#{user.profile.city.toLowerCase()}/problem-name') Broken Bench
            td 105 Oak Rd.
            td submitted 5 minutes ago
            td
              a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
          tr
            td
              a(href='/#{user.profile.city.toLowerCase()}/problem-name') Unpainted Fire Hydrant
            td 1300 Maple St.
            td submitted yesterday
            td
              a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
    .col-md-6
      h2 Quick Look
      table.table.table-hover
        thead
          tr
            th Problem
            th Location
            th Pledge Amount
            th Time Pledged
            th View
        tbody
          tr
            td
              a(href='/#{user.profile.city.toLowerCase()}/problem-name') Broken Bench
            td 105 Oak Rd.
            td $20
            td submitted 5 minutes ago
            td
              a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
          tr
            td
              a(href='/#{user.profile.city.toLowerCase()}/problem-name') Unpainted Fire Hydrant
            td 1300 Maple St.
            td $50
            td submitted yesterday
            td
              a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
