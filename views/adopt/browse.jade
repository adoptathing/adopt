extends ../layout

block content
  .page-header
    h1 
      #{user.profile.city}&nbsp;
    span.h3 Browse Problems
  .row
    .col-sm-12
      input.form-control(type='text' placeholder='Search for a Problem')
      br
      br
    .col-sm-6
      .panel.panel-default
        .panel-body
          h3
            a(href='/#{user.profile.city.toLowerCase()}/problem-name') Broken Bench
          p 105 Oak Rd.
          p $20
          p submitted 5 minutes ago
          p
            a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
      .panel.panel-default
        .panel-body
          h3
            a(href='/#{user.profile.city.toLowerCase()}/problem-name') Unpainted Fire Hydrant
          p 1300 Maple St.
          p $50
          p submitted yesterday
          p
            a.btn.btn-xs.btn-info(href='/#{user.profile.city.toLowerCase()}/problem-name') View &raquo;
    .col-sm-6
      p Google Maps here with images (like AirBnb)
      #map
  script.
    function initMap() {
      // Create a map object and specify the DOM element for display.
      var map = new google.maps.Map(document.getElementById('map'), {
        center: {lat: -34.397, lng: 150.644},
        scrollwheel: false,
        zoom: 8
      });
    }